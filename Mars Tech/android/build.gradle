buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:7.3.0" // Downgraded for compatibility
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
